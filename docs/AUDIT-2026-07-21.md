# Who's On First — Audit 2026-07-21

## What changed since 2026-07-14

No code changes. One docs commit (`c986b7e`) wrote the 2026-07-14 audit. All
previously identified findings remain open; this audit re-confirms them and marks their status.

---

## Critical

None identified.

---

## High

None identified.

---

## Medium

### M-1 · `/api/practice/generate-station` does not check approved status — carry-over from 2026-07-14
**File:** `src/app/api/practice/generate-station/route.ts:47`
**Status:** **Not fixed.** Pending users can trigger Claude Haiku calls.
**Fix:** Call `getUser(request)` and verify `user.status === 'approved'` before proceeding.

### M-2 · No rate limiting on expensive third-party routes — carry-over from 2026-07-14
**Files:** `src/app/api/practice/generate-station/route.ts`, `src/app/api/spotify/sync-playlist/route.ts`
**Status:** **Not fixed.** Both make outbound Anthropic / Spotify calls without a throttle.

### M-3 · `logoDataUrl` stored without server-side size cap — carry-over from 2026-07-14
**File:** `src/app/api/roster/route.ts` (PUT handler)
**Status:** **Not fixed.** Arbitrarily large base64 payloads can be written to Postgres.
**Fix:** Reject `logoDataUrl` values longer than ~2.7 MB (≈ 2 MB binary → base64) with HTTP 413.

### M-4 · `constraint_overrides` table is dead code — carry-over from 2026-07-14
**File:** `src/lib/schema.ts`, `drizzle/` migrations
**Status:** **Not fixed.** Table exists in schema and migrations but is never read or written.
**Fix:** Drop in a new Drizzle migration; remove from `schema.ts`.

---

## Low

### L-1 · `practice-pdf.ts` duplicates `loadPennant` — carry-over from 2026-07-14
**File:** `src/lib/practice-pdf.ts`, `src/lib/pdf.ts`
**Status:** **Not fixed.**

### L-2 · No Content-Security-Policy header — carry-over from 2026-07-14
**File:** `next.config.ts`
**Status:** **Not fixed.**

### L-3 · `storage.ts` localStorage helpers are stale — carry-over from 2026-07-14
**File:** `src/lib/storage.ts`
**Status:** **Not fixed.**

### L-4 · No test suite — carry-over from 2026-07-14
**Status:** **Not fixed.**
