# Who's On First — Audit 2026-09-01

## What changed since 2026-08-25

No application code was committed between 2026-08-25 and this audit. Five code
fixes and one dependency update cycle were applied this cycle — see Applied Fixes
below.

---

## Critical

None identified.

---

## High

### H-1 · Next.js 15.5.15 contains multiple patched CVEs — new
**Files:** `package.json`, `package-lock.json`

`npm audit` reports 14+ advisories against `next@15.5.15`, including:

- **GHSA-492v-c6pp-mqqv** — Middleware/Proxy bypass via dynamic route parameter injection
- **GHSA-267c-6grr-h53f** / **GHSA-26hh-7cqf-hhc6** — Segment-prefetch Middleware bypass
- **GHSA-wfc6-r584-vfw7** — Cache poisoning in React Server Component responses
- **GHSA-c4j6-fc7j-m34r** — SSRF via WebSocket upgrade
- **GHSA-3g8h-86w9-wvmq** — Middleware redirect cache poisoning
- **GHSA-8h8q-6873-q5fj** — DoS via Server Components
- Multiple further cache-confusion, SSRF, and DoS advisories

All of these are patched in the 15.5.x line. Fix: upgrade to `next@15.5.25`
(or the latest 15.x patch). This is a same-minor patch upgrade — no breaking
changes are expected.

**Status: Fixed this cycle** — `npm audit fix` upgraded `next` 15.5.15 → 15.5.25
along with `postcss`, `nanoid`, `js-yaml`, `brace-expansion`, and `sharp`.
TypeScript check (`npx tsc --noEmit`) passed after the upgrade.

---

## Medium

### M-2 · No rate limiting on any API route — carry-over
**Files:** `src/app/api/**`

No rate-limiting middleware exists. Every API route accepts unlimited request
volume. The two most exposed routes:
- `POST /api/spotify/sync-playlist` — for each player without a walk-on song, it
  iterates a pool of default candidates calling `findCleanTrack()` per entry.
  In the worst case (13 playerless players × 20-song pool) this is up to 260
  sequential Spotify search calls in a single request.
- `POST /api/practice/generate-station` — proxies to Anthropic API per request.

Both can be triggered by any approved coach with no throttle.

**Fix:** Add per-IP or per-user rate limiting (e.g. `@upstash/ratelimit` with
Vercel KV). Sync-playlist and generate-station should have tighter limits
(e.g. 5 req/min) than read-only roster/history endpoints (e.g. 60 req/min).

---

### M-3 · Logo data-URL accepted with no server-side size cap — carry-over
**File:** `src/app/api/roster/route.ts` (PUT handler, `config?.logoDataUrl` path, ~line 112)

Team logos stored as base64 data URIs have no server-side maximum. An oversized
upload can bloat the `teams` DB row and potentially exceed Neon's request payload
limit. The Onboarding UI caps file selection at 2 MB, but the API itself enforces
nothing, so a direct API caller bypasses this.

**Fix:** Reject `logoDataUrl` values whose `length` exceeds ~270 000 characters
(approximately 200 KB of binary content after base64 overhead). Return HTTP 413.

---

### M-4 · `constraint_overrides` table is completely unused — carry-over
**Files:** `src/lib/schema.ts`, `drizzle/0001_multi_team.sql`

The `constraint_overrides` table exists in the schema and migrations but is
never queried by any route or UI component. It is dead weight at the DB layer
and creates confusion for future contributors about whether it is intentional.

**Fix:** Drop the table in a new migration and remove the schema definition,
or wire the overrides into the solver's constraint pipeline if the feature is
actively planned.

---

### M-5 · AI model ID hardcoded in source — carry-over
**File:** `src/app/api/practice/generate-station/route.ts:86`

`claude-haiku-4-5-20251001` is a literal string in the route handler. When
Anthropic retires the model ID the endpoint silently returns 502 errors. There
is no equivalent of the `check:models` script used in the sibling
`escape-hatch-explore` project. (Previously M-7.)

**Fix:** Extract to a named constant in a shared `lib/ai.ts` file. Add a weekly
CI step (or `npm run check:models` script) that calls the Anthropic models list
endpoint and verifies the model ID is still active.

---

## Low

### L-1 · Remaining CVEs in bundled next/postcss and dev esbuild — new
**Files:** `package.json`, `package-lock.json`

After `npm audit fix`, 6 advisories remain:

| Package | Severity | Notes |
|---|---|---|
| `esbuild` (via `drizzle-kit`) | Moderate | Dev-only migration tool; no production exposure |
| `postcss` (bundled inside `next`) | Moderate | Requires Next.js 16 to fix — breaking change |

The `next/node_modules/postcss` issue is the internal postcss bundled by the
Next.js build toolchain. It is not the application-facing postcss used for
Tailwind styles; it affects the dev server's CSS processing.

**Fix:** Evaluate upgrading to Next.js 16 on the next planned major-version
cycle. For drizzle-kit, monitor for a patch release that pins esbuild ≥ 0.24.3
and upgrade when available. Neither issue warrants an emergency fix.

---

### L-2 · No Content Security Policy header — carry-over
**File:** `next.config.ts`

The app sets `X-Frame-Options`, `X-Content-Type-Options`, and `Referrer-Policy`
but no CSP. User-supplied content (team names, player names, logo data URIs)
is rendered in the DOM. Any future `dangerouslySetInnerHTML` path would create
an XSS vector with no CSP backstop.

**Fix:** Add a CSP via `next.config.ts` headers block. At minimum:
`default-src 'self'; img-src 'self' data: https://open.spotify.com; script-src 'self' 'unsafe-inline'`
(tighten further as the app's content sources are fully inventoried).

---

### L-3 · `storage.ts` is dead code — carry-over
**File:** `src/lib/storage.ts`

`loadRoster` / `saveRoster` (localStorage) are no longer called from production
code. `page.tsx` still imports `addHistoryEntry` and `clearAbsences` from this
module, but those functions are pure logic helpers that never touch localStorage.

**Fix:** Extract `addHistoryEntry` and `clearAbsences` as pure functions into
a small `lib/history-helpers.ts` (or similar), then delete `storage.ts` entirely.
This removes dead localStorage code paths and makes the module's import surface clearer.

---

### L-4 · No test suite — carry-over
**Files:** `src/lib/scheduler.ts`, `src/lib/constraints.ts`

The backtracking scheduler and constraint logic are pure functions ideal for
unit testing but have zero coverage. An edge-case constraint change could
silently produce invalid game sheets.

**Fix:** Add `vitest` and cover the scheduler with scenario tests: minimum
bench minutes, max-per-week caps, playoff mode bench ordering, avoid-positions
post-pass, 10-player vs 13-player roster sizes.

---

## Applied fixes this cycle

All fixes verified with `npx tsc --noEmit` (exit 0).

### Fix 1 — H-1: Next.js upgraded from 15.5.15 to 15.5.25
**File:** `package-lock.json`

`npm audit fix` was run and upgraded:
- `next` 15.5.15 → 15.5.25 (patches 14+ Middleware, cache-poisoning, SSRF, and DoS advisories)
- `postcss` 8.5.8 → 8.5.26
- `nanoid` 3.3.11 → 3.3.18
- `js-yaml` 4.1.1 → 4.3.2
- `brace-expansion` 5.0.5 → 5.0.9
- `sharp` → 0.35.x (with updated libvips)

Remaining moderate-severity advisories require either Next.js 16 (breaking) or a
drizzle-kit patch and are tracked as L-1.

### Fix 2 — L-7: Admin GET user list unbounded
**File:** `src/app/api/admin/users/route.ts:42`

Added `.limit(500)` to the `db.select().from(users)` query in the GET handler,
with a code comment noting cursor pagination should be added if the limit is
ever reached. The response shape is unchanged.

### Fix 3 — L-8: spotify-status response leaked internal serviceUserId
**File:** `src/app/api/auth/spotify-status/route.ts:14–19`

Removed `serviceUserId` from the JSON response. The endpoint now returns only
`{ configured, linked }`. No client code referenced `serviceUserId` for display
or routing — confirmed by searching all client-side component and page files.

### Fix 4 — L-9: Teams DELETE used a full row-fetch for count guard
**File:** `src/app/api/teams/[id]/route.ts:100–103`

Replaced:
```typescript
const count = await db.select({ id: teams.id }).from(teams).where(eq(teams.userId, userId));
if (count.length <= 1) { … }
```
with:
```typescript
const [{ n }] = await db
  .select({ n: sql<number>`count(*)::int` })
  .from(teams)
  .where(eq(teams.userId, userId));
if (n <= 1) { … }
```
Added `sql` to the drizzle-orm import accordingly.

### Fix 5 — L-10: Middleware HMAC used a manual XOR loop instead of crypto.subtle.verify
**File:** `src/middleware.ts:30–48`

Replaced the manual base64url encode-then-XOR-compare block with
`crypto.subtle.verify`, which performs the constant-time byte comparison
internally and is the idiomatic Web Crypto approach in the Edge runtime.
The key import was changed from `["sign"]` to `["verify"]`. The session
format (userId.timestamp.hmac in a base64url wrapper) is unchanged; only
the verification path was updated.

### Fix 6 — New: Admin POST/PATCH stored `name` without sanitization
**File:** `src/app/api/admin/users/route.ts:71–75` (POST), `~147` (PATCH)

The `name` field accepted by both the POST (create user) and PATCH (update user)
admin handlers was stored verbatim with no HTML stripping or length cap, while
the roster PUT (`coachName`) already applied both (fixed in 2026-08-25 L-5).

Applied the same guard to both admin handlers:
```typescript
typeof name === "string"
  ? name.replace(/<[^>]*>/g, "").slice(0, 100).trim() || null
  : null
```
This matches the sanitization pattern used consistently across other user-facing
string fields.

---

## Open findings summary

| ID   | Severity | Category        | Status               |
|------|----------|-----------------|----------------------|
| H-1  | High     | Security (CVE)  | **Fixed 09-01**      |
| M-2  | Medium   | Security        | Not fixed            |
| M-3  | Medium   | Security        | Not fixed            |
| M-4  | Medium   | Maintainability | Not fixed            |
| M-5  | Medium   | Maintainability | Not fixed            |
| L-1  | Low      | Security (CVE)  | **New, not fixed**   |
| L-2  | Low      | Security        | Not fixed            |
| L-3  | Low      | Maintainability | Not fixed            |
| L-4  | Low      | Testing         | Not fixed            |
| L-7  | Low      | Performance     | **Fixed 09-01**      |
| L-8  | Low      | Security        | **Fixed 09-01**      |
| L-9  | Low      | Performance     | **Fixed 09-01**      |
| L-10 | Low      | Security        | **Fixed 09-01**      |
| L-new| Low      | Security        | **Fixed 09-01**      |
